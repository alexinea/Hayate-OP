<Project Sdk="Microsoft.NET.Sdk">

    <Import Project="..\..\..\common.tests.props" />

    <!-- ================================================================
         LEGACY 世代测试项目（net48 冒烟 + net6/net7 兼容覆盖）
         - 目标框架：net48;net6.0;net7.0（net48 为 S2 向下兼容冒烟，xunit v2 原生支持）
         - 测试框架：xunit v2 + VSTest（库项目，由 dotnet test 注入 runner）
         - 与 tests/HayateOP.Tests（future, net8/9/10 + v3 + MTP）保持用例同步。
           新增用例需在 future 与 legacy 同步维护（除非某代不适用某特性）。
    ================================================================ -->
    <PropertyGroup>
        <TargetFrameworks>net48;net6.0;net7.0</TargetFrameworks>
        <RootNamespace>DotNetCore.HayateOP.Tests</RootNamespace>
        <IsPackable>false</IsPackable>
        <!-- Z1: the specialized package's net48 asset carries System.Memory (the Span surface of
             HayateValueStringBuilder); reference the same version here and generate binding redirects
             so System.Buffers / System.Memory do not load mixed versions. -->
        <AutoGenerateBindingRedirects>true</AutoGenerateBindingRedirects>
        <GenerateBindingRedirectsOutputType>true</GenerateBindingRedirectsOutputType>
    </PropertyGroup>

    <ItemGroup Condition="'$(TargetFramework)' == 'net48'">
        <PackageReference Include="System.Memory" Version="4.5.5" />
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="coverlet.collector" Version="8.0.0">
            <PrivateAssets>all</PrivateAssets>
            <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
        </PackageReference>
        <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.13.0" />
        <PackageReference Include="Moq" Version="4.20.72" />
        <PackageReference Include="xunit" Version="2.9.3" />
        <PackageReference Include="xunit.runner.visualstudio" Version="3.0.2">
            <PrivateAssets>all</PrivateAssets>
            <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
        </PackageReference>
    </ItemGroup>

    <ItemGroup>
      <ProjectReference Include="..\..\..\src\HayateOP\HayateOP.csproj" />
      <ProjectReference Include="..\..\..\src\HayateOP.Extensions.Specialized\HayateOP.Extensions.Specialized.csproj" />
      <ProjectReference Include="..\..\..\src\HayateOP.Extensions.Buffers\HayateOP.Extensions.Buffers.csproj" />
    </ItemGroup>

    <ItemGroup>
        <Using Include="Xunit" />
    </ItemGroup>

</Project>
